<?xml version="1.0" encoding="UTF-8"?>

<widget xmlns     = "http://www.w3.org/ns/widgets"
        xmlns:gap = "http://phonegap.com/ns/1.0"
        id        = "com.crmobile.cardsolitaire"
        version   = "0.0.1">

    <name>4 Card Solitaire</name>

    <description>
        4 Card Solitaire is the funnest and simplest solitaire game ever!
    </description>

    <author href="https://play.google.com/store/apps/details?id=com.crmoblie.cardsolitaire&hl=en" email="clintj42@gmail.com">
        C R Mobile
    </author>

    <preference name="permissions"                value="none"/>

    <!-- Customize your app and platform with the preference element. -->
    <preference name="orientation"                value="portrait" />        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device"              value="universal" />      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen"                 value="true" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="webviewbounce"              value="false" />           <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="prerendered-icon"           value="true" />           <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="stay-in-webview"            value="false" />          <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="detect-data-types"          value="true" />           <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="exit-on-suspend"            value="false" />          <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="show-splash-screen-spinner" value="true" />           <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen"    value="false" />           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="disable-cursor"             value="false" />          <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->

    <!-- Define app icon for each platform. -->
    <icon src="Images/buttons/icon.png" />
    <icon src="Images/buttons/icon.png" gap:platform="android" gap:qualifier="ldpi" />
    <icon src="Images/buttons/icon.png" gap:platform="android" gap:qualifier="mdpi" />
    <icon src="Images/buttons/icon.png" gap:platform="android" gap:qualifier="hdpi" />
    <icon src="Images/buttons/icon.png" gap:platform="android" gap:qualifier="xhdpi" />
    <icon src="Images/buttons/icon.png" gap:platform="blackberry" />
    <icon src="Images/buttons/icon.png" gap:platform="blackberry" gap:state="hover"/>
    <icon src="Images/buttons/icon.png" gap:platform="ios" width="57" height="57" />
    <icon src="Images/buttons/icon.png" gap:platform="ios" width="72" height="72" />
    <icon src="Images/buttons/icon.png" gap:platform="ios" width="114" height="114" />
    <icon src="Images/buttons/icon.png" gap:platform="ios" width="144" height="144" />
    <icon src="Images/buttons/icon.png" gap:platform="webos" />
    <icon src="Images/buttons/icon.png" gap:platform="winphone" />
    <icon src="Images/buttons/icon.png" gap:platform="winphone" gap:role="background" />

    <access origin="*"/>
</widget>